# Versiones FIJADAS a un stack estable y compatible entre sí.
# Motivo (2026-07-10): sin fijar, Streamlit Cloud instalaba lo más nuevo
# (pandas 3.0 + numpy 2.5 + pyarrow 25 + streamlit 1.59) y la app crasheaba
# con "Segmentation fault" al serializar dataframes (choque binario pyarrow/numpy).
# Este set evita ese bleeding-edge. No subir a pandas 3.x / numpy 2.5 sin probar.
streamlit==1.39.0
pandas==2.2.3
numpy==1.26.4
pyarrow==16.1.0
plotly==5.24.1
gspread==6.1.4
google-auth==2.35.0
